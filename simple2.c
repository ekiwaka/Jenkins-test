#include <stdio.h>
int product(int x, int y) {
   // printf() displays the product of two numbers
   printf("%d", x*y);
   return 0;
}


int main(){
	int var;
	printf("Enter a number");
	scanf("%d", &var)
	product (3,var);
	return 0;
}